<li class='contact'>
  <%= f.hidden_field '_destroy' %>
  <%= f.hidden_field :contact_id, :class => 'contact_id' %>

  <div class='role'>
    <%= f.label 'Role' %>

    <%= f.select :role, current_account.roles, :placeholder => 'Role', :class => 'role small' %>
  </div>

  <div class='name'>
    <%= f.label 'Contact' %>

    <%= fields_for :contact do |builder| %>
      <%= builder.select :name, options_from_collection_for_select(current_user.contacts, 'id', 'name') %>
    <% end %>
  </div>

  <%= link_to 'x', '#', :class => 'delete' %>
</li>
